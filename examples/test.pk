extern puts(x:int, y:int) end
extern puti(num:int) end
extern putf(num:float) end

struct Vec2
    x: int
    y: int
end

pub proc main() do
    p: Vec2 = {5, 6}
    puti(p@x)
    puti(p@y)
    p@x = 10;
    p@y = 11;
    puti(p@x)
    puti(p@y)
end