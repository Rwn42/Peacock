extern puts(x:int, y:int) end
extern puti(x:int) end

MyConst: int : 10 2 +;

proc add(x: int, y: int) int do
    return x y +
end

pub proc main() do
    i: int = 0;
    while i < 10 do
        puti(i)
        i = add(i, 1);
    end
end